let formidable;_9e6‍.w("formidable",[["default",["formidable"],function(v){formidable=v}]]);var express = require("express");
var router = express.Router();
// 導入


/* GET home page. */
router.get("/", function (req, res, next) {
  res.render("index", { title: "Express" });
});

// 顯示網頁的 (表單)
router.get("/portrait", (req, res) => {
  res.render("portrait");
});

// 處理文件上傳
router.post("/portrait", (req, res) => {
  // 創建 form 對象
  const form = formidable({
    // 設置上傳文件的保存目錄
    // uploadDir
  });
  // 解析請求報文
  form.parse(req, (err, fields, files) => {
    if (err) {
      next(err);
      return;
    }
    _9e6‍.g.console.log(fields); // text radio checkbox select
    _9e6‍.g.console.log(files); // file

    res.send("OK");
    // res.json({ fields, files });
  });
});

module.exports = router;
